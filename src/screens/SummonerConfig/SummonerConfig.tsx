import { ServerSelection } from '../../components/templates/ServerSelection';
import { Wrapper } from '../../components/templates/Wrapper';

export const SummonerConfig = () => {
  return (
    <Wrapper backButton>
      <ServerSelection title="Edite seu" />
    </Wrapper>
  );
};
